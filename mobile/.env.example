# Whisper Flutter app environment template
# Copy this file to `.env` and adjust values per environment.

# API Configuration
# Base URL for the NestJS backend API (e.g., http://localhost:3001 or https://api.example.com)
API_URL=http://localhost:3001
# HTTP request timeout in milliseconds
API_TIMEOUT=30000

# Stellar Network Configuration
# Network to use: "testnet" for development, "public" for mainnet
STELLAR_NETWORK=testnet
# Horizon endpoint: testnet => https://horizon-testnet.stellar.org, public => https://horizon.stellar.org
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org

# Application Configuration
# Environment name: development | staging | production
APP_ENV=development
# Enable verbose logging and debugging features in development
DEBUG_MODE=true

# WebSocket Configuration
# WebSocket endpoint for real-time features (e.g., ws://localhost:3001 or wss://api.example.com)
WS_URL=ws://localhost:3001
